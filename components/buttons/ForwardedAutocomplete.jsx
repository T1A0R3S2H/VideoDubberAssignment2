import { Autocomplete } from "@mantine/core";

// You can try adding the unstyled prop here if needed:
const ForwardedAutocomplete = (props) => {
  return <Autocomplete unstyled {...props} />;
};

export default ForwardedAutocomplete;
